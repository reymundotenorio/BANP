%li.dropdown
  %a.dropdown-toggle{"data-toggle": "dropdown", href: "#"}
    %span.visible-xs
      = t("views.navigation.user")
      %i.fa.fa-angle-down
    %span.hidden-xs
      %i.fas.fa-user
  %ul.dropdown-menu.user-dropbown
    - if current_costumer
      -# Costumer detail Sync
      = sync partial: "costumer_info", resource: current_costumer
      = sync_new partial: "costumer_info", resource: Costumer.new
      -# End Costumer detail Sync

    - else
      %li
        = link_to (sign_in_path) do
          %i.fas.fa-sign-in-alt.hidden-xs
          = t("header.user.sign_in")
      %li
        = link_to (sign_up_path) do
          %i.fas.fa-user-plus.hidden-xs
          = t("header.user.sign_up")
