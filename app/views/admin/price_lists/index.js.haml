-# Render count and title
$("#count-title").html("#{escape_javascript(render "count_title")}");
-# Render table
$("#render-data").html("#{escape_javascript(render "sales")}");
-# Render pagination
$(".pagination a").attr("data-remote", "true");
