.row.no-margin
  .col-xs-12.no-padding
    -# Table
    %table.table
      %thead
        - model = "sale"
        - attributes = ["sale_datetime", "total_sale", "customer", "employee", "payment_method"]

        -# Render table header
        = render partial: "partials/table_header", locals: {model: model, attributes: attributes}

      %tbody
        -# Data table render_sync
        = sync partial: "price_lists_data_table", collection: @price_lists
        = sync_new partial: "price_lists_data_table", resource: Sale.new
        -# End Data table render_sync
    -# End Table


.row.no-margin
  .col-xs-12.no-padding.text-center
    -# Pagination
    = will_paginate @price_lists, renderer: BootstrapPagination::Rails
    -# End Pagination
