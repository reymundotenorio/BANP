.row.no-margin
  .col-xs-12.no-padding
    -# Table
    %table.table
      %thead
        - model = "sale"
        - attributes = ["sale_datetime", "total_sale", "customer", "employee", "paid", "payment_method", "delivery_status"]

        -# Render table header
        = render partial: "partials/table_header", locals: {model: model, attributes: attributes}

      %tbody
        -# Data table render_sync
        = sync partial: "shipments_data_table", collection: @shipments
        = sync_new partial: "shipments_data_table", resource: Sale.new
        -# End Data table render_sync
    -# End Table


.row.no-margin
  .col-xs-12.no-padding.text-center
    -# Pagination
    = will_paginate @shipments, renderer: BootstrapPagination::Rails
    -# End Pagination
