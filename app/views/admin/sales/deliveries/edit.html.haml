-# Form Title
%section.form-title
  .container
    -# Title
    .row.title-container
      .col-xs-12.col-sm-4.col-lg-8
        -# Heading
        %h1{class: "#{@delivery.state}"}
          %i.fas.fa-edit
          = t("views.edit.edit")
        -# End Heading

      .col-xs-12.col-sm-8.col-lg-4
        -# Provider button
        .btn-container
          = link_to (admin_sale_details_path(@delivery.id)), class: "btn btn-general" do
            %i.fas.fa-id-card
            %span= t("sale.delivery_details")
        -# End Provider button
    -# End Title

    -# Breadcrumb
    .row
      .col-xs-12.hidden-xs
        .breadcrumb-container
          %ol.breadcrumb
            %li
              = link_to (admin_root_path) do
                %i.fas.fa-tachometer-alt
                %span= t("header.navigation.dashboard")
            %li
              = link_to (admin_sale_deliveries_path) do
                %i.far.fa-newspaper
                %span= t("sale.deliveries")
            %li
              = link_to (admin_sale_details_path(@delivery.id)) do
                %i.fas.fa-list-alt
                = "#{t('sale.delivery_details')} ##{@delivery.id}"
            %li.active
              %i.fas.fa-edit
              = t("views.edit.edit")
    -# End Breadcrumb
-# End Form Title

-# Render form
= render "admin/sales/deliveries/form"
-# End Render form
