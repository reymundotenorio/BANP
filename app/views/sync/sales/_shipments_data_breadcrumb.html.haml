.row
  .col-xs-12.hidden-xs
    .breadcrumb-container
      %ol.breadcrumb
        %li
          = link_to (admin_root_path) do
            %i.fas.fa-tachometer-alt
            %span= t("header.navigation.dashboard")
        %li
          = link_to (admin_sale_shipments_path) do
            %i.far.fa-newspaper
            %span= t("sale.shipments")
        %li.active
          %i.fas.fa-list-alt
          = "#{@count == 1 ? t('sale.shipment_detail') : t('sale.shipment_details')} ##{sale.id}"
